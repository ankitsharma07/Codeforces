#include <iostream>
#include <vector>

using namespace std;

class ModuloSum {
public:
        bool modulo_sum(vector<int>& nums) {

        }
};

int main() {
    int n, m;
    vector<int> arr;
    cin >> n >> m;

    for(int i = 0; i < n; i++){
        arr.push_back(i);
    }
}
